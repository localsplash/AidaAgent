# Use the SAME LiveKit project as OfficePulse; do not commit real credentials.
LIVEKIT_URL=wss://officepulse-localsplash-dev-wx1v0ch5.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
# Must match OfficePulse LIVEKIT_AGENT_NAME. Stop the previous aida-prime worker
# before assigning this name, or use aida-prime-dev in BOTH deployments.
AIDA_AGENT_NAME=aida-prime-bootstrap-dev

# Explicit LiveKit Inference choices; per-call metadata cannot override these.
AIDA_STT_MODEL=deepgram/nova-3-general
AIDA_LLM_MODEL=google/gemma-4-31b-it
AIDA_TTS_MODEL=deepgram/aura-2
AIDA_TTS_VOICE=asteria

# REQUIRED: OfficePulse's public HTTPS origin serving /v1/agent/calls/{id}/bootstrap,
# no path or query (dev: https://officepulse-api.localsplash.dev). The worker refuses
# to start while it is empty or an example.* placeholder.
AIDA_BOOTSTRAP_URL=
# Must match the LiveKit trunk header-to-attribute mapping for X-Aida-Route-Token.
AIDA_ROUTE_TOKEN_ATTRIBUTE=sip.aidaRouteToken
# Total join + SIP wait + authorization + session start + ready deadline (1-60 seconds).
AIDA_BOOTSTRAP_TIMEOUT_SECONDS=30

# Internal application diagnostics; independent of the SDK health listener on 8081.
AIDA_STATUS_HOST=0.0.0.0
AIDA_STATUS_PORT=8082

# Optional: attach to an existing reverse-proxy Docker network (compose.proxy.yaml)
# so the proxy can reach /healthz by container name. Compose reads these from .env.
# COMPOSE_FILE=compose.yaml:compose.proxy.yaml
# AIDA_PROXY_NETWORK=npm_network
